SYSTEM_PROMPT = """당신은 한국 보험 사업방법서(별지) 문서를 분석하는 전문가입니다.
마크다운으로 변환된 문서 내 표에서 담보별 파라미터(보험기간, 납입기간, 가입연령)를 찾아 채우는 것이 임무입니다.

핵심 도메인 지식:
- 담보명: 표의 '구분' 컬럼에서 확인. 중복 허용.
- 보기(보험기간): 값과 구분코드로 분리
  - 보기구분: 1=년만기(예: 10년, 20년), 2=세만기(예: 80세만기, 100세만기), 3=종신
- 납기(납입기간): 값과 구분코드로 분리
  - 납기구분: 1=년납(예: 10년, 20년, 전기납), 2=세납(예: 60세납), 3=일시납
- 시작연령/끝연령: 가입 가능 연령 범위
  - 시작연령유형/끝연령유형: 1=세(단순 숫자), 기타=수식이 포함된 경우

값 해석 규칙:
- "090" = 90세만기 → 보기: 090, 보기구분: 2
- "100" = 100세만기 → 보기: 100, 보기구분: 2
- "020" = 20년납 → 납기: 020, 납기구분: 1
- 연령범위 "16세~40세" → 시작연령: 016, 끝연령: 040, 유형 모두 1
- 수식이 포함된 경우 "41세~MIN(기본계약 만기(세)-만기(연),80세)" → 시작연령: 041, 유형: 1, 끝연령은 수식 그대로 유지

출력 시 숫자값은 3자리로 맞춤 (예: 16 → 016, 90 → 090).
"""

BATCH_EXTRACTION_PROMPT = """아래는 보험 사업방법서(별지)에서 추출한 마크다운 문서(표 포함)입니다.
주어진 담보 목록의 각 담보에 대해, 문서 내 표를 참조하여 파라미터를 채워주세요.

## 문서 내용 (마크다운 표 포함)
{document_markdown}

## 채워야 할 담보 목록 ({batch_size}건)
아래 담보들의 보기/납기/연령 정보를 문서에서 찾아 채워주세요.
각 담보는 경우의 수(보기×납기×연령)에 따라 여러 행으로 전개됩니다.

{coverage_list}

## 추출 규칙

1. **담보명 매칭**: 위 목록의 담보명과 문서 내 표의 '구분' 컬럼 값을 매칭합니다.
   - 정확히 일치하지 않을 수 있으므로, 유사한 이름도 매칭하세요.
   - 예: "유사암최초수술비" ↔ "유사암 최초 수술비"

2. **병합셀 처리**: 빈 셀은 바로 위 행의 같은 컬럼 값을 상속합니다.

3. **연령범위 해석**:
   - "16세~40세" → 시작연령: 016, 시작연령유형: 1, 끝연령: 040, 끝연령유형: 1
   - 수식 포함 시 → 수식 그대로, 유형은 별도 표기

4. **보험기간(보기) 해석**:
   - 숫자+세만기 → 보기구분: 2 (예: 90세만기 → 보기: 090, 보기구분: 2)
   - 숫자+년 → 보기구분: 1 (예: 10년 → 보기: 010, 보기구분: 1)
   - 종신 → 보기구분: 3

5. **납입기간(납기) 해석**:
   - 숫자+년납/년 → 납기구분: 1 (예: 20년납 → 납기: 020, 납기구분: 1)
   - 전기납 → 납기구분: 1, 납기값은 보기값과 동일
   - 일시납 → 납기구분: 3

6. **경우의 수 전개**: 하나의 담보에 여러 보기/납기/연령 조합이 있으면 각각 별도 행으로 출력합니다.
   예: 보기가 [90세, 100세], 납기가 [20년, 25년, 30년]이면 2×3=6행

7. **문서에서 찾을 수 없는 담보**: 해당 담보의 모든 필드를 빈 문자열("")로 출력합니다.

## 출력 형식
반드시 아래 JSON 배열 형식으로 출력하세요. 다른 텍스트 없이 JSON만 출력합니다.
상품코드와 담보PMID는 입력에서 그대로 가져오세요.

```json
[
  {{
    "상품코드": "입력 그대로",
    "담보PMID": "입력 그대로",
    "담보명": "입력 그대로",
    "담보구분": "0 또는 해당 코드",
    "보기": "3자리 숫자 문자열",
    "보기구분": "1 또는 2 또는 3",
    "납기": "3자리 숫자 문자열",
    "납기구분": "1 또는 2 또는 3",
    "시작연령": "3자리 숫자 문자열",
    "시작연령유형": "1 또는 수식설명",
    "끝연령": "3자리 숫자 문자열 또는 수식",
    "끝연령유형": "1 또는 수식설명"
  }}
]
```
"""

FALLBACK_BATCH_PROMPT = """이전 추출 시도에서 유효한 JSON을 생성하지 못했습니다.
아래 문서와 담보 목록을 참고하여 반드시 유효한 JSON 배열로만 응답하세요.
다른 설명 텍스트 없이 [ ... ] 형태의 JSON만 출력합니다.

## 문서 내용
{document_markdown}

## 담보 목록
{coverage_list}

## 필수 필드
상품코드, 담보PMID, 담보명, 담보구분, 보기, 보기구분, 납기, 납기구분, 시작연령, 시작연령유형, 끝연령, 끝연령유형
"""
